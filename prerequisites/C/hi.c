#include <stdio.h>
int main()
{
    /*
     variable declarations must come before other types of statements in the given "code block"
    */
    printf("I am alive! Beware. \n");
    getchar(); //reads a single character and waits for user to hit enter. 
    return 0;
}